/**
  * GeneratorItem - container class for line item in generator
  */
interface GeneratorItem {
    // class members
    prodcd: string;
    productname: string;
    price: number;
    ext: number;
    qty: number;
}